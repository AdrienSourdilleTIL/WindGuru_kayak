spot:
  id: 48552
  name: "La Couarde sur Mer"
  island: "Île de Ré"
  lat: 46.19
  lon: -1.42
  model: "gfs"
  variables:
    - WSPD
    - GUST
    - WDIRN
    - HTSGW
    - PERPW
    - TMP
    - APCP1

fishing:
  timezone: "Europe/Paris"
  hours_start: 6
  hours_end: 20
  forecast_days: 14

scoring:
  weights:
    wind: 0.25         # Vent moyen (nœuds) — idéal < 15 kts
    gust: 0.20         # Rafales (nœuds) — idéal < 17 kts
    wave_height: 0.20  # Hauteur vagues (m) — idéal < 1.2m
    wave_period: 0.15  # Période vagues (s) — idéal > 12s
    rain: 0.10         # Précipitations (mm/h)
    temperature: 0.10  # Température (°C)
  verdicts:
    excellent: 70
    favorable: 50
    moyen: 30

email:
  smtp_host: "smtp.gmail.com"
  smtp_port: 587
  # Credentials via variables d'environnement : GMAIL_USER, GMAIL_APP_PASSWORD
